# 英语星球 PWA 版 - 使用指南

## 🎉 这是什么？

这是一个 **PWA（渐进式 Web 应用）**，可以像原生 App 一样安装到 iPhone 主屏幕，**完全免费、永久有效、不需要签名、不需要 Xcode、不需要开发者账号**。

### 核心优势
- ✅ **永久使用**：没有 7 天签名过期问题
- ✅ **完全离线**：添加到主屏幕后，断网也能正常学习
- ✅ **全屏运行**：没有浏览器地址栏，和原生 App 体验一样
- ✅ **免费托管**：用 Netlify Drop 拖拽上传，免费永久
- ✅ **所有功能**：单词战斗、听力、跟读、阅读、语法、错题本、白天/夜晚模式

---

## 📦 第一步：免费托管（3 种方式，任选其一）

### 方式一：Netlify Drop（最简单，推荐）

1. 用 Mac 或电脑浏览器打开：**https://app.netlify.com/drop**
2. 把 `PWA版本` 这个**整个文件夹**拖到网页中间的虚线框里
3. 等待几秒钟，会得到一个网址，类似 `https://random-name.netlify.app`
4. **建议注册一个免费账号**（用 GitHub 或邮箱登录），这样网址永久有效，不会过期
5. 注册后可以在 Site settings 里修改网址名字，比如 `english-planet.netlify.app`

### 方式二：GitHub Pages（适合有 GitHub 账号的）

1. 在 GitHub 创建一个新仓库（Public）
2. 把 `PWA版本` 里的所有文件上传到仓库根目录
3. 仓库 Settings → Pages → Source 选 `main` 分支 → Save
4. 等 1-2 分钟，得到网址 `https://你的用户名.github.io/仓库名/`

### 方式三：Vercel / Cloudflare Pages

类似 Netlify，注册免费账号后导入文件夹即可。

---

## 📱 第二步：安装到 iPhone（30 秒完成）

1. 用 iPhone 的 **Safari 浏览器**（必须是 Safari，Chrome 不支持添加到主屏幕）打开你托管后的网址
2. 等待页面完全加载
3. 点击 Safari 底部的 **分享按钮**（方框带向上箭头的图标）
4. 向下滑动，找到并点击 **"添加到主屏幕"**
5. 右上角点 **"添加"**
6. 回到主屏幕，你会看到"英语星球"的图标
7. 点击图标，应用会**全屏启动**，没有浏览器地址栏

---## ✈️ 第三步：验证离线可用

1. 安装完成后，打开一次应用（确保所有资源缓存到手机）
2. 打开 iPhone 的**飞行模式**（或关闭 Wi-Fi 和蜂窝数据）
3. 再次点击主屏幕的"英语星球"图标
4. 应用应该能正常打开，所有功能都能用
5. 学习进度、错题本都会保存在手机本地

> **原理**：Service Worker 会在第一次打开时把所有资源（1.5MB 的 HTML + 图标）缓存到手机本地，之后打开直接从缓存读取，完全不需要网络。

---

## 🔄 更新应用

当我更新了应用版本后：
1. 把新的 `PWA版本` 文件夹重新上传到 Netlify（覆盖旧文件）
2. 在 iPhone 上打开应用，连网状态下会自动检测更新
3. 完全关闭应用（从后台划掉），再重新打开就是新版本
4. 学习进度和错题本**不会丢失**（保存在本地 localStorage）

---

## ⚙️ 功能说明

### 已实现的全部功能
| 功能 | 说明 |
|------|------|
| 🗺️ 剧情地图 | 5 级词汇，从高频到低频，解锁式学习 |
| ⚔️ 单词战斗 | 4 种题型：看英选中、看中选英、听音选词、拼写 |
| 📕 错题本 | 答错自动入库，正确率越低出现越频繁 |
| 👂 听力练习 | 可理解输入，听音选义 |
| 🗣️ 影子跟读 | 调速播放，语音识别打分（需联网+Safari支持） |
| 📖 阅读语法 | 分级短文 + 高频语法点 |
| 🤖 AI 陪练 | DeepSeek API，可选（需联网+API Key） |
| ☀️🌙 白天/夜晚 | 自动根据本机时间切换（6:00-18:00 白天） |
| 📱 横竖屏 | 自动适配，横屏双栏布局 |
| 💾 本地存储 | 所有进度、错题、设置保存在手机本地 |
| 👨‍👩‍👧 双档案 | 成人/孩子独立进度 |
| 🌏 双语种 | 英语 + 日语 |

### 错题本逻辑
- 答错一次 → 自动加入错题本
- 正确率 0% → 出现权重 10（最常出现）
- 正确率 50% → 出现权重 5
- 正确率 80% → 出现权重 2
- 正确率 100% → 出现权重 0.5（最少出现）
- 可以在错题本里单独复习、筛选、移除、清除已掌握

---

## ❓ 常见问题

**Q：为什么必须用 Safari？Chrome 不行吗？**
A：iOS 的"添加到主屏幕"和 Service Worker 离线缓存只有 Safari 支持。Chrome 等第三方浏览器在 iOS 上受系统限制，不支持 PWA 安装。

**Q：跟读打分功能能用吗？**
A：iOS Safari 14.5+ 支持语音识别，但需要联网。如果不需要打分，只听发音跟读是完全离线的。

**Q：AI 陪练功能需要联网吗？**
A：是的，AI 陪练调用 DeepSeek API，需要联网和 API Key。不填 Key 也能用内置例句。其他所有功能完全离线。

**Q：换手机了怎么办？**
A：学习进度保存在旧手机本地，换手机需要重新开始。未来可以加云同步功能。

**Q：Netlify 免费吗？会收费吗？**
A：Netlify 免费额度足够个人使用（100GB 流量/月），你的应用只有 1.5MB，完全不会超。

**Q：可以修改应用名字和图标吗？**
A：可以。修改 `manifest.json` 里的 `name` 和 `short_name`，替换 `icons/` 文件夹里的图标即可。

---

## 📁 文件结构

```
PWA版本/
├── index.html          # 主应用（1.5MB，含全部词库和功能）
├── manifest.json       # PWA 应用清单
├── sw.js               # Service Worker（离线缓存）
├── favicon.png         # 网站图标
├── icon.png            # 原始图标（1024x1024）
└── icons/
    ├── icon-192.png    # 小图标
    ├── icon-512.png    # 中图标
    └── icon-1024.png   # 大图标
```
